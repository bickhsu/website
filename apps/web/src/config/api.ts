/**
 * API Global Config
 */
export const API_BASE_URL = 'http://localhost:3000/api/v1';

export const ENDPOINTS = {
  SEQUENCES: `${API_BASE_URL}/sequence`,
  KEYFRAMES: `${API_BASE_URL}/keyframe`,
  UPLOAD: `${API_BASE_URL}/upload/image`,
};
